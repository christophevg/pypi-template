# NOTE: This project is DEPRECATED
# Migrate to pyproject.toml + hatchling setup
# See: c3:python-project skill

[tool.ruff]
extend-exclude = [ "pypi_template/templates", "docs" ]

[tool.coverage.run]
omit = [ "*/templates/*", "*/.tox/*", "*/distutils/*", "tests/*" ]

[tool.coverage.report]
omit = [ "*/templates/*", "*/.tox/*", "*/distutils/*", "tests/*" ]
